model CategoryHousehold {
    id        String      @id @default(auto()) @map("_id") @db.ObjectId

    name      String      @unique

    products  Product[]  // Relation avec vos produits ménagers
}